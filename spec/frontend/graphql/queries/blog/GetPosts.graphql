query GetPosts {
  strapi_posts(pagination: {page: 5, pageSize: 6}) {
    data {
      id
      attributes {
        content
        title
        createdAt
      }
    }
    meta {
      pagination {
        page
        pageCount
        total
      }
    }
  }
}